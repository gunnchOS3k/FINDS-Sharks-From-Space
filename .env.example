# Environment

# Web (public)
VITE_API_BASE=http://127.0.0.1:8787

# Worker secrets — put these in api/worker/.dev.vars or `wrangler secret`, never in VITE_* 
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
ALLOWED_ORIGINS=http://localhost:3000,https://finds-web.pages.dev
