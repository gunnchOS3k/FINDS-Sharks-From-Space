# Contributing

1. Use `cursor/…` or `feat/…` branches. Do not commit `.env`, `.dev.vars`, keystores, or APK signing material.
2. Keep claims scientifically honest. Live coordinates must come from NASA-derived cells.
3. Run `npm run verify` before opening a PR.
4. Pixel 6a validation docs must never include device serials.
