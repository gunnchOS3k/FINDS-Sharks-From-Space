name: Deploy Discord Bot (disabled)

# Obsolete: FINDS production closeout removed this workflow from the active pipeline.
# The discord-bot/ directory remains for historical reference only.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Discord bot deploy is disabled for FINDS production closeout."
